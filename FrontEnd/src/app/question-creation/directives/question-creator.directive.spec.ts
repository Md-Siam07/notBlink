import { QuestionCreatorDirective } from './question-creator.directive';

describe('QuestionCreatorDirective', () => {
  it('should create an instance', () => {
    const directive = new QuestionCreatorDirective();
    expect(directive).toBeTruthy();
  });
});
